import React from "react"
import PortalLayout from "../components/portal/portal-layout"
// const { useLocation } = require("gatsby")

export default function AllPokemon(props) {
  //   const { post } = pageContext

  return (
    <PortalLayout>
      <div></div>
    </PortalLayout>
  )
}
